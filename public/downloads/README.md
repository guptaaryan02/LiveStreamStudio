# LiveStream Studio downloads

Place release installers here before deploying the public website:

- `LiveStream-Studio-macOS.dmg` - present
- `LiveStream-Studio-Windows.exe` - pending Windows build
- `LiveStream-Studio-Linux.AppImage` - pending Linux build

The landing page routes available downloads to the support page after click. Pending platforms show a build-pending button instead of a dead download.
